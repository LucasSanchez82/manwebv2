import { describe, it } from 'bun:test'

describe('provider.film', () => {
  it('should return all content type ids available', () => {
    fetch('https://anime-sama.fr/template-php/defaut/fetch.php', {})
  })
})
